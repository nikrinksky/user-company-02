spring:
  application:
    name: user-service
#  config:
#    import: 'optional:configserver:'
  cloud:
    config:
      uri: http://localhost:8888

spring.config.import: 'optional:configserver:'


#  config:
#    import: optional:configserver




