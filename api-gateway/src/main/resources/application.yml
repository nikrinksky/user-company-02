server:
  port: 8089 # Уникальный порт для api-gateway для реализации config-service через docker-compose

spring:
  application:
    name: api-gateway
  cloud:
    config:
      uri: http://config-service:8888
spring.config.import: 'optional:configserver:'