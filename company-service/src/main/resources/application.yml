server:
  port: 8082  # Уникальный порт для company-service для реализации config-service через docker-compose

spring:
  application:
    name: company-service
  #  config:
  #    import: 'optional:configserver:'
  cloud:
    config:
      uri: http://config-service:8888
spring.config.import: 'optional:configserver:'
