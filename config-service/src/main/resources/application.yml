# config-service/application.yml для реализации через docker-compose
server:
  port: 8888  # Важно: именно этот порт используется в контейнере

spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: https://github.com/nikrinksky/user-company-config-repo.git
#          clone-on-start: true
#          default-label: main
#          search-paths:
#            - '{application}/{profile}'
#            - '{application}'
#            - default

eureka:
  client:
    service-url:
      defaultZone: http://discovery-service:8761/eureka/
#    register-with-eureka: true
#    fetch-registry: true
#  instance:
#    prefer-ip-address: true
#    hostname: ${HOSTNAME:localhost}

#    healthcheck:
#      enabled: true
#  instance:
#    hostname: ${HOSTNAME:localhost}
#    preferIpAddress: true
